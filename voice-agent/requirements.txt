cartesia-line>=0.2.16
# Transitive via cartesia-line, but imported directly here for the usage callback — declared so a
# future cartesia-line that drops or vendors it fails at install rather than at the first call.
# The range must stay inside cartesia-line's own (litellm<=1.84,>=1.83.0) or pip refuses to resolve.
litellm>=1.83,<=1.84
httpx>=0.27
uvicorn>=0.30
